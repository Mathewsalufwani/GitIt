import { CompleteDatePipe } from './complete-date.pipe';

describe('CompleteDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CompleteDatePipe();
    expect(pipe).toBeTruthy();
  });
});
